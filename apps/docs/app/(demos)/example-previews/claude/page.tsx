import { Claude } from "@/components/examples/claude";
import { ThreadPreviewPage } from "../thread-preview-page";

export default function Page() {
  return <ThreadPreviewPage component={Claude} />;
}
