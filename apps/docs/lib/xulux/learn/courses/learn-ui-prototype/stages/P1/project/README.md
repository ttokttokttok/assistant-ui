# Learn Mode prototype — P1

This is the complete first-assistant stage used by the assistant-ui Learn Mode prototype.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Add an OpenAI API key to `.env.local` before sending a message.
