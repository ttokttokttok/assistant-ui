import { Assistant } from "../components/assistant";

export default function Page() {
  return (
    <main className="h-screen bg-[var(--background)] text-[var(--foreground)]">
      <Assistant />
    </main>
  );
}
