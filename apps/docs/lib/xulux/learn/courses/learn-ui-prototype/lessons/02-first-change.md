# First change

The second stage replaces the static welcome page with a minimal assistant built from assistant-ui thread, message, and composer primitives.

Compare `app/page.tsx` with the previous stage, then inspect the newly added `components/assistant.tsx`. The preview and downloadable project resolve from these same canonical files.
